{
  "name": "accounting-ledger",
  "version": "1.1.0",
  "description": "An accounting ledger app.",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox",
    "watch": "watch 'npm run build' .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "electron-builder --win"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "watch": "^1.0.2",
    "papaparse": "^5.4.1"
  },
  "build": {
    "appId": "com.example.accountingledger",
    "productName": "Accounting Ledger",
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
